langague: java

script:
  - mvn dependency:go-offline
  - mvn clean verify

env:
  global:
    - secure: $DOCKER_PASS

services:
  - docker

before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "arthurliger" --password-stdin

cache:
  directories:
    - $HOME/.m2
